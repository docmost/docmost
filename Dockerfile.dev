# 开发热加载用：仅装 Node + pnpm，代码通过 volume 挂载
FROM node:22-slim
RUN apt-get update \
    && apt-get install -y --no-install-recommends procps \
    && rm -rf /var/lib/apt/lists/* \
    && npm install -g pnpm@10.4.0
WORKDIR /app
