# DocOps — Services Module (Backend)

NestJS module for Service Catalog management.

Spec: §3.1 — Modulo Gestione catalogo servizi.
Entity: `services`, `service_tags`, `tags`.
API: `POST/GET/PATCH/DELETE /api/v1/services`.
