import * as React from 'react';
interface Props {
    actorName: string;
    pageTitle: string;
    spaceName: string;
    pageUrl: string;
    comment?: string;
}
export declare const ApprovalRejectedEmail: ({ actorName, pageTitle, spaceName, pageUrl, comment, }: Props) => React.JSX.Element;
export default ApprovalRejectedEmail;
