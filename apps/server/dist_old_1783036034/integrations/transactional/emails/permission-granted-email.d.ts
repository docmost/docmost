import * as React from 'react';
interface Props {
    actorName: string;
    pageTitle: string;
    pageUrl: string;
    accessLabel: string;
}
export declare const PermissionGrantedEmail: ({ actorName, pageTitle, pageUrl, accessLabel, }: Props) => React.JSX.Element;
export default PermissionGrantedEmail;
