import * as React from 'react';
interface Props {
    username: string;
    resetLink: string;
}
export declare const ForgotPasswordEmail: ({ username, resetLink }: Props) => React.JSX.Element;
export default ForgotPasswordEmail;
