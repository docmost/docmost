import * as React from 'react';
interface Props {
    actorName: string;
    pageTitle: string;
    pageUrl: string;
}
export declare const CommentMentionEmail: ({ actorName, pageTitle, pageUrl, }: Props) => React.JSX.Element;
export default CommentMentionEmail;
