import * as React from 'react';
interface Props {
    userName: string;
    actorName: string;
    pageTitle: string;
    pageUrl: string;
    spaceName: string;
}
export declare const PageUpdateEmail: ({ userName, actorName, pageTitle, pageUrl, spaceName, }: Props) => React.JSX.Element;
export default PageUpdateEmail;
