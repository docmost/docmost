import * as React from 'react';
interface Props {
    pageTitle: string;
    spaceName: string;
    pageUrl: string;
}
export declare const VerificationExpiredEmail: ({ pageTitle, spaceName, pageUrl }: Props) => React.JSX.Element;
export default VerificationExpiredEmail;
