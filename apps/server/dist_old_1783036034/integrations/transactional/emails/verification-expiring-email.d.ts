import * as React from 'react';
interface Props {
    pageTitle: string;
    spaceName: string;
    pageUrl: string;
    expiresAt: string;
}
export declare const VerificationExpiringEmail: ({ pageTitle, spaceName, pageUrl, expiresAt, }: Props) => React.JSX.Element;
export default VerificationExpiringEmail;
