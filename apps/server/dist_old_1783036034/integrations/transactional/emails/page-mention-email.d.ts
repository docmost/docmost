import * as React from 'react';
interface Props {
    actorName: string;
    pageTitle: string;
    pageUrl: string;
}
export declare const PageMentionEmail: ({ actorName, pageTitle, pageUrl }: Props) => React.JSX.Element;
export default PageMentionEmail;
