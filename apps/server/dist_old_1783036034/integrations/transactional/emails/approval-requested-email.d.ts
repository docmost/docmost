import * as React from 'react';
interface Props {
    actorName: string;
    pageTitle: string;
    spaceName: string;
    pageUrl: string;
}
export declare const ApprovalRequestedEmail: ({ actorName, pageTitle, spaceName, pageUrl, }: Props) => React.JSX.Element;
export default ApprovalRequestedEmail;
