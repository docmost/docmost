import type { Extensions } from '@tiptap/core';
import { type ParseOptions } from '@tiptap/pm/model';
export declare function generateJSON(html: string, extensions: Extensions, options?: ParseOptions): Record<string, any>;
