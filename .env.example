# your domain, e.g https://example.com
APP_URL=http://localhost:3000
PORT=3000

# minimum of 32 characters. Generate one with: openssl rand -hex 32
APP_SECRET=REPLACE_WITH_LONG_SECRET

# Google Workspace SSO (self-hosted)
GOOGLE_SSO_ENABLED=false
GOOGLE_SSO_CLIENT_ID=
GOOGLE_SSO_CLIENT_SECRET=
GOOGLE_SSO_ALLOWED_DOMAINS=
GOOGLE_SSO_ALLOW_SIGNUP=false
GOOGLE_SSO_ENFORCE=false
GOOGLE_SSO_DISPLAY_NAME=Google Workspace

JWT_TOKEN_EXPIRES_IN=30d

DATABASE_URL="postgresql://postgres:password@localhost:5432/docmost?schema=public"
REDIS_URL=redis://127.0.0.1:6379

# --- Deploy em VM com docker-compose.prod.yml ---
# As variáveis abaixo NÃO são lidas pela aplicação; servem apenas para a
# interpolação do serviço `db` em docker-compose.prod.yml (Postgres em container).
# Ao usar o compose de produção, DATABASE_URL deve apontar para o host `db`
# (nome do serviço), ex: postgresql://docmost:SENHA_FORTE@db:5432/docmost
# e POSTGRES_PASSWORD tem que ser a MESMA senha usada em DATABASE_URL.
POSTGRES_DB=docmost
POSTGRES_USER=docmost
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# options: local | s3 | azure
STORAGE_DRIVER=local

# S3 driver config
AWS_S3_ACCESS_KEY_ID=
AWS_S3_SECRET_ACCESS_KEY=
AWS_S3_REGION=
AWS_S3_BUCKET=
AWS_S3_ENDPOINT=
AWS_S3_FORCE_PATH_STYLE=

# Azure Blob Storage driver config
AZURE_STORAGE_ACCOUNT_NAME=
AZURE_STORAGE_ACCOUNT_KEY=
AZURE_STORAGE_CONTAINER=

# default: 50mb
FILE_UPLOAD_SIZE_LIMIT=

# options: smtp | postmark
MAIL_DRIVER=smtp
MAIL_FROM_ADDRESS=hello@example.com
MAIL_FROM_NAME=Docmost

# SMTP driver config (SendPulse: host smtp-pulse.com)
# Port options: 587 (TLS/STARTTLS, SMTP_SECURE=false) | 465 (SSL, SMTP_SECURE=true) | 2525 (STARTTLS, SMTP_SECURE=false)
SMTP_HOST=smtp-pulse.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_SECURE=false
SMTP_IGNORETLS=false

# Postmark driver config
POSTMARK_TOKEN=

# for custom drawio server
DRAWIO_URL=

# Gotenberg URL for server-side PDF export
GOTENBERG_URL=

DISABLE_TELEMETRY=false

# Allow other sites to embed Docmost in an iframe.
IFRAME_EMBED_ALLOWED=false

# Only used when IFRAME_EMBED_ALLOWED=true. When empty, any origin is allowed.
# Example: https://intranet.example.com,https://portal.example.com
IFRAME_ALLOWED_ORIGINS=

# Enable debug logging in production (default: false)
DEBUG_MODE=false

# Log database queries
DEBUG_DB=false

# Log http requests
LOG_HTTP=false

# Unlock EE feature flags on a self-hosted instance. CSV of feature keys, or "*" for all.
# WARNING: EE features are under the Docmost Enterprise License; production use needs a
# valid subscription. Most features have no server engine bundled (SSO/SCIM/templates/MCP
# are cosmetic here), and enforce-MFA does NOT actually enforce a second factor.
# Only "retention" and "sharing:controls" have real server behavior in this build.
# Leave empty to keep everything locked (default).
SELF_HOSTED_UNLOCK_FEATURES=
